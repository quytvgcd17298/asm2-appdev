@model GES_APP.Models.Topic
@{ ViewBag.Title = "Edit"; }

<h2>Edit</h2>

@Html.ValidationSummary(false, "", new { @class = "text-danger" })
@using (Html.BeginForm("Details", "Categories", FormMethod.Post))
{
@Html.HiddenFor(u => u.Id)
                <div class="form-horizontal">
                    <hr />
                    <dl class="dl-horizontal">
                        <dt>@Html.DisplayNameFor(u => u.Name)</dt>
                        <dd>
                            @Html.DisplayFor(u => u.Name)
                        </dd>
                        <dt>
                            @Html.DisplayNameFor(u => u.Description)
                        </dt>
                        <dd>
                            @Html.DisplayFor(u => u.Description)
                        </dd>
                    </dl>
                </div>}

<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
